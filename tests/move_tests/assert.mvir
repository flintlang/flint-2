import {{default}}.Assert;

main(account: &signer) {
    Assert.publish(copy(account));
    Assert.shouldNotCrash(copy(account));
    //! expect fail 7
    Assert.shouldCrash(copy(account));
    return;
}