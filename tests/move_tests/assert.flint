contract Assert {
    var dummy: Int = 0
}

Assert :: (any) {
    public init() {}

    public func shouldCrash() {
        assert(false)
    }

    public func shouldNotCrash() {
        assert(true)
    }
}