struct Flint_Global {
    func balanceOf(account: Address) -> Int {
      return Flint_balanceOf(account)
    }

    func transfer(from: Address, to: Address, amount: Int) {
      Flint_transfer(from, to, amount)
    }
}