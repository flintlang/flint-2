struct Flint_Global {
    public func balanceOf(account: Address) -> Int {
      return Flint_balanceOf(account)
    }

    public func transfer(from: Address, to: Address, amount: Int) {
      Flint_transfer(from, to, amount)
    }
}